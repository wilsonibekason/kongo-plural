import { fs } from "fs";
import { path } from "path";

class CartItems {
  static addProductsToCart({ ...params }, id, productPrice) {
    /// fetch previous cart items
    /// analyse cart => check if cart exists
    // increate item quantity, price, and total Quantty ==>  add cartitem to the cart  model
  }
}
